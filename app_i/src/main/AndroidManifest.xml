<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.recoveryrecord.surveyandroid.example">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:label="BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name = "android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Widget.SurveyAndroid">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name = ".MyNotificationPublisherNews" />

        <activity android:name=".ExampleSurveyActivity" />
        <activity android:name=".NewsModuleActivity" />
        <activity android:name=".NewsMainActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".NetworkCheckerActivity" />
        <receiver android:name=".receiever.ApplicationSelectorReceiver"></receiver>
        <receiver android:name=".receiever.ScreenStateReceiver"></receiver>
<!--        <receiver android:name=".NotificationReceiver"></receiver>-->
        <receiver android:name=".receiever.ApplicationSelectorReceiver"></receiver>
        <receiver android:name=".receiever.BlueToothReceiver"></receiver>

        <activity android:name=".AddNotificationActivity"></activity>
        <activity android:name=".activity.NotificationDbViewActivity"></activity>
        <activity android:name=".ESMDbViewActivity"></activity>
        <activity android:name=".ESMJsonViewActivity"></activity>
        <receiver android:name=".receiever.NetworkChangeReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
        <activity android:name=".NotificationSettingActivity" />
        <service android:name=".MyNotificationListenerService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>